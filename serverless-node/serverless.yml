plugins:
  - serverless-plugin-typescript
  - serverless-offline

service: nodelambda

provider:
  name: aws
  region: us-east-1
  runtime: nodejs10.x
  stage: lab

functions:
  hello:
    handler: src/handler.hello
    events:
      - http:
          path: hello
          method: get
          cors: true